name: Fetch Data from Local API

on:
  schedule:
    # Her 5 dakikada bir çalıştır
    - cron: '*/5 * * * *'
  push:
    branches: [ main ]
  workflow_dispatch:  # Manuel çalıştırma için

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
    
    - name: Set up Python for error handling
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Create public directory if not exists
      run: |
        mkdir -p public
    
    - name: Fetch API Data with retry logic
      id: fetch-data
      run: |
        echo "🔄 Local API'den veri çekiliyor..."
        echo "🌐 API URL: http://192.168.1.3:3000/api/emirler"
        
        # Retry mekanizması (3 deneme)
        for i in {1..3}; do
          echo "🔁 Deneme $i/3..."
          
          # Curl ile API'den veri çek
          API_RESPONSE=$(curl -s \
            --max-time 30 \
            --retry 2 \
            --retry-delay 5 \
            --retry-max-time 60 \
            -H "User-Agent: GitHub-Actions-Fetcher/1.0" \
            -H "Accept: application/json" \
            -H "X-Requested-With: TrueSignal-Data-Fetch" \
            "http://192.168.1.3:3000/api/emirler" || echo "CURL_ERROR")
          
          # Eğer curl başarılıysa devam et
          if [ "$API_RESPONSE" != "CURL_ERROR" ] && [ ! -z "$API_RESPONSE" ]; then
            echo "✅ API'den veri başarıyla alındı (${#API_RESPONSE} karakter)"
            
            # JSON geçerlilik kontrolü
            if echo "$API_RESPONSE" | python3 -m json.tool > /dev/null 2>&1; then
              echo "✅ JSON formatı geçerli"
              
              # Veriyi kaydet
              echo "$API_RESPONSE" > public/data.json
              
              # Metadata ekle
              TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
              echo '{ "success": true, "lastFetch": "'$TIMESTAMP'", "source": "http://192.168.1.3:3000", "emirler": '"$API_RESPONSE"' }' > public/api-data.json
              
              # İstatistikleri hesapla
              COUNT=$(echo "$API_RESPONSE" | python3 -c "import sys, json; data=json.load(sys.stdin); print(len(data) if isinstance(data, list) else 0)")
              echo "📊 $COUNT emir bulundu"
              
              echo "success=true" >> $GITHUB_OUTPUT
              echo "count=$COUNT" >> $GITHUB_OUTPUT
              break
            else
              echo "❌ Geçersiz JSON formatı"
            fi
          else
            echo "❌ API'ye bağlanılamadı (Deneme $i)"
            sleep 5
          fi
          
          if [ $i -eq 3 ]; then
            echo "❌ Tüm denemeler başarısız, fallback veri kullanılacak"
          fi
        done
        
        # Eğer tüm denemeler başarısız olursa fallback veri oluştur
        if [ ! -f public/data.json ]; then
          echo "⚠️  Fallback veri oluşturuluyor..."
          
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          
          # Fallback veri (API down olduğunda)
          FALLBACK_DATA='[
            {
              "Id": 999,
              "Tarih/Saat": "'$(date +"%Y-%m-%d %H:%M:%S")'",
              "Sembol": "API_UNAVAILABLE",
              "Tip": "INFO",
              "Lot": 0,
              "GirisFiyati": 0,
              "StopLoss": 0,
              "TakeProfit": 0,
              "KapanisFiyati": 0,
              "RSI": 0,
              "MACD": "API Kapalı",
              "EMA": "API Kapalı",
              "STOACH": "API Kapalı",
              "Durum": "API Kapalı",
              "ListType": "Sistem",
              "KarZarar": 0,
              "KarZararYuzde": 0
            }
          ]'
          
          echo "$FALLBACK_DATA" > public/data.json
          echo '{ "success": false, "lastFetch": "'$TIMESTAMP'", "source": "fallback", "message": "API temporarily unavailable", "emirler": '"$FALLBACK_DATA"' }' > public/api-data.json
          
          echo "success=false" >> $GITHUB_OUTPUT
          echo "count=1" >> $GITHUB_OUTPUT
        fi
    
    - name: Validate JSON files
      run: |
        echo "🔍 JSON dosyaları kontrol ediliyor..."
        
        # api-data.json kontrolü
        if [ -f public/api-data.json ]; then
          echo "📁 api-data.json:"
          python3 -m json.tool public/api-data.json > /dev/null && echo "✅ api-data.json geçerli" || echo "❌ api-data.json geçersiz"
          echo "📏 Boyut: $(wc -c < public/api-data.json) bytes"
        fi
        
        # data.json kontrolü
        if [ -f public/data.json ]; then
          echo "📁 data.json:"
          python3 -m json.tool public/data.json > /dev/null && echo "✅ data.json geçerli" || echo "❌ data.json geçersiz"
          echo "📏 Boyut: $(wc -c < public/data.json) bytes"
        fi
        
        # Örnek veriyi göster
        echo "📋 Örnek veri (ilk 500 karakter):"
        head -c 500 public/api-data.json && echo "..."
    
    - name: Create README for public folder
      run: |
        cat > public/README.md << 'EOF'
        # Public Data Files
        
        Bu klasör GitHub Actions tarafından otomatik olarak oluşturulur.
        
        ## Dosyalar
        
        - `data.json`: Ham API verisi
        - `api-data.json`: Metadata ile zenginleştirilmiş API verisi
        
        ## Veri Akışı
        
        1. GitHub Actions her 5 dakikada bir çalışır
        2. Local API'den (http://192.168.1.3:3000) veri çeker
        3. JSON formatında bu klasöre kaydeder
        4. GitHub Pages üzerinden servis edilir
        
        ## API Endpoint
        
        ```json
        GET https://cagatay-a.github.io/TrueSignals/api-data.json
        ```
        
        ## Örnek Yanıt
        
        ```json
        {
          "success": true,
          "lastFetch": "2024-01-18T12:00:00Z",
          "source": "http://192.168.1.3:3000",
          "emirler": [...]
        }
        ```
        
        ## Manuel Tetikleme
        
        GitHub Actions sayfasından "Run workflow" butonuna tıklayarak manuel tetikleyebilirsiniz.
        EOF
    
    - name: Commit and Push changes
      id: commit-push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "💾 Değişiklikler commit ediliyor..."
        
        # Git ayarları
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Değişiklikleri kontrol et
        git status
        
        # Değişiklikleri ekle
        git add public/
        
        # Commit mesajı oluştur
        TIMESTAMP=$(date +"%Y-%m-%d %H:%M:%S")
        if [ "${{ steps.fetch-data.outputs.success }}" = "true" ]; then
          COMMIT_MSG="📊 API Verileri Güncellendi (${{ steps.fetch-data.outputs.count }} emir) - $TIMESTAMP"
        else
          COMMIT_MSG="⚠️  Fallback Veri Kullanıldı (API Kapalı) - $TIMESTAMP"
        fi
        
        # Commit yap
        git commit -m "$COMMIT_MSG" || echo "⚠️  Commit yapılamadı (muhtemelen değişiklik yok)"
        
        # Push yap
        echo "🚀 Değişiklikler push ediliyor..."
        git push origin main
        
        echo "✅ İşlem tamamlandı"
        
        # Sonuçları output'a ekle
        echo "commit_message=$COMMIT_MSG" >> $GITHUB_OUTPUT
    
    - name: Summary
      run: |
        echo "🎉 GitHub Actions İşlemi Tamamlandı"
        echo "====================================="
        echo "📅 Zaman: $(date)"
        echo "✅ Başarılı: ${{ steps.fetch-data.outputs.success }}"
        echo "📊 Emir Sayısı: ${{ steps.fetch-data.outputs.count }}"
        echo "💾 Commit: ${{ steps.commit-push.outputs.commit_message }}"
        echo "🌐 Erişilebilir: https://cagatay-a.github.io/TrueSignals/api-data.json"
        echo "📱 Sayfa: https://cagatay-a.github.io/TrueSignals/"
        echo "====================================="
        
        # Son 5 commit'i göster
        echo "📝 Son 5 commit:"
        git log --oneline -5
        
        # Dosya durumunu göster
        echo "📁 Public klasörü içeriği:"
        ls -la public/